# TalentForge MCP Server — Variáveis de Ambiente
# Copie este arquivo para .env e preencha com os valores reais
# NUNCA commite o .env com valores reais

# ── Supabase ──────────────────────────────────────────────────────────────────
# URL do projeto Supabase (disponível em: Project Settings > API)
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxxx.supabase.co

# Service Role Key — bypassa RLS para operações de sistema
# ATENÇÃO: nunca exponha esta chave no frontend ou em logs públicos
# Disponível em: Project Settings > API > service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
