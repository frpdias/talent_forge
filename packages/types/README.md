# Shared Types

Coloque aqui tipos comuns entre `api` e `web` (DTOs, enums). Ex.: `ApplicationStatus`, `OrgType`, payloads de API.

Sugestão: publicar como package interno ou consumir via imports relativos no monorepo.
